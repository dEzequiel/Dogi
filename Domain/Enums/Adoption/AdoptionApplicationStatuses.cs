namespace Domain.Enums.Adoption;

/// <summary>
/// Represents options which an adoption application can have.
/// </summary>
public enum AdoptionApplicationStatuses
{
    Waiting = 1,
    Accepted = 2,
    Declined = 3
}